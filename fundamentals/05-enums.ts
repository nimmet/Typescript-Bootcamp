
enum CourseType {
    FREE = "FREE",
    PREMIUM="PREMIUM",
    PRIVATE="PRIVATE",
    HIDDEN="HIDDEN"
}

const course = {
    title: "Java Spring MVC",
    type:CourseType.FREE
}

console.log(course);
