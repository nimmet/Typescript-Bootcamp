const courseName = "TypeScript Bootcamp";

printCourseName(courseName);
printCourseName("Java Spring");

function printCourseName(name :string){
    console.log("The course name is: "+name.toUpperCase());
}